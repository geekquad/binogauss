from setuptools import setup

setup(name='binogauss',
      version='0.1',
      description='Gaussian and Binomial distribution',
      packages=['binogauss'],
      author='geekquad', 
      author_email = 'adityaastranaut@gmail.com',
      zip_safe=False)
